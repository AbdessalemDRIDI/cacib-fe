import { Injectable } from '@angular/core';
import { PartnerPartnerViewBaseService } from './partner-partner-view.service-base';

/**
 * PartnerPartnerViewService
 * This service extends the PartnerPartnerViewBaseService and used to override
 * some generated functionalities if needed.
 *
 * @AutoSkip Do not remove this tag to keep your file out of regeneration.
 * @Note This service is generated once and will no more be erased by the generator.
 */
@Injectable()
export class PartnerPartnerViewService extends PartnerPartnerViewBaseService {}
