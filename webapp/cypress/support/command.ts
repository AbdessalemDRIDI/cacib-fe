/***
 * Write here your Cypress global Commands that can be used in your Tests cases
 * like basic Login access
 * */
/*
Cypress.Commands.add('login', (username, password) => {
    cy.visit('http://localhost:4200/login')
    cy.get('[data-test=admin]').type(username)
    cy.get('[data-test=admin]').type(password)
    cy.get('#login').click()
    cy.url().should('contain', '/home')
}) */
